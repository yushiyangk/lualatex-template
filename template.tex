\documentclass[11pt, a4paper, british]{article}

% Intended to be compiled with LuaLaTeX; assumes UTF-8 support

\usepackage{yushiyang}

%%%%%%%%%%
% CONTENTS
% Navigate to a section quickly by searching for the %-tag
% - Optional packages (%opt)
% - Specialised packages (%sp)
% - Language support (%lang)
% - Word count configuration (%wc)
% - Drafting annotations (%dra)
% - Squeeze (%sq)
% - Document configuration (%conf)
% - Preamble, document-specific definitions (%pre)
% - Document (%doc)


%%%%%%%%%%opt
% OPTIONAL PACKAGES

%% Page layout
%\usepackage{lua-visual-debug}  % Activate this package to draw boxes and rules in output for debugging
%\usepackage{parskip}  % Activate this package to add spacing between paragraphs and remove indentation
%\usepackage{titlepic}  % Provide a title page picture for article, report or book document classes
%\usepackage{calc}  % Enable arithmetic on lengths and measuring lengths from content
%\usepackage{setspace}  % Enable line spacing adjustments
%\usepackage{changepage}  % Enable margin adjustments and the adjustwidth environment
%\usepackage{pdfpages} % Enable inclusion of entire PDF pages
%\usepackage[bottom]{footmisc}  % Force footnotes to very bottom of pages, and below bottom floats (enable this if disabling /flushbottom)

%% Tables and figures
%\usepackage{rotfloat}  % Provide rotated floats
%\usepackage{wrapfig}  % Provide tables and figures that are wrapped around by text
%\usepackage{tcolorbox}  % Provide highlighted boxes

%% Sections and linking
%\usepackage{appendix}  % Enable appendix layout adjustments and provide section or part-specific appendices with the subappendices environment
%\usepackage{embedfile}  % Support embedding files in the PDF output
%\usepackage{hypgotoe}  % Provide hyperlinks to embedded PDFs in the PDF output

%% Citations
%\usepackage{yushiyang-biblatex} % Provide automatically formatted citations and references sections


%%%%%%%%%%sp
% SPECIALISED PACKAGES

%% Graphics
%\usepackage{yushiyang-tikz}  % Provide general vector graphics (basis of most embedded graphics packages)
%\usepackage{yushiyang-pgfplots}  % Provide function or data plots

%% Chemistry
%\usepackage{mhchem}  % Provide chemical formulae and equations

%% Electronics
%\usepackage{yushiyang-circuitikz}  % Provide circuit diagrams

%% Software
%\usepackage{yushiyang-minted} % Provide source code environment with syntax highlighting (requires --shell-escape option, and requires Python package Pygments; pip install Pygments)
%\usepackage[os=win]{menukeys}  % Provide formatted menu sequences and key strokes

%% Statistics and machine learning
%\usepackage{yushiyang-bayesnet} % Provide Bayesian networks (https://github.com/jluttine/tikz-bayesnet)

%% Management
%\usepackage{yushiyang-pgfgantt}  % Provide Gantt charts


%%%%%%%%%%lang
% LANGUAGE SUPPORT

%\babelfont[chinese-simplified]{rm}[Path=babel-fonts/,Extension=.ttc,UprightFont=*-Regular,BoldFont=*-Bold]{SourceHanSerif}  % Simplified Chinese
%\babelfont[chinese-traditional]{rm}[Path=babel-fonts/,Extension=.ttc,UprightFont=*-Regular,BoldFont=*-Bold]{SourceHanSerif}  % Traditional Chinese
%\babelfont[japanese]{rm}[Path=babel-fonts/,Extension=.ttc,UprightFont=*-Regular,BoldFont=*-Bold]{SourceHanSerif}  % Japanese
%\babelfont[korean]{rm}[Path=babel-fonts/,Extension=.ttc,UprightFont=*-Regular,BoldFont=*-Bold]{SourceHanSerif}  % Korean


%%%%%%%%%%wc
% WORD COUNT CONFIGURATION

% TeXcount instructions start with %TC:
% To disable, insert a space between % and TC, as in % TC:

%% Do not count document title
%TC:macro \title [ignore]

%% Treat maths environments from package amsmath like other maths environments
%TC:envir gather [] displaymath

%% Count code blocks as display math
%TC:envir minted [ignore] displaymath
%TC:macro \mint [ignore,displaymath]
%TC:macro \mintinline [ignore,inlinemath]

%TC:macro \textcolor [ignore,text]
%TC:macro \newgeometry [ignore]


%%%%%%%%%%dra
% DRAFTING ANNOTATIONS (enable during drafting only)

%% Show text block, header, footer and sidebar boundaries and crop marks (package geometry)
%\geometry{showframe, showcrop}

%% Colour hyperlinks (package hyperref)
%\hypersetup{colorlinks=false}

%% Disable bookmark generation for quicker builds (package bookmark)
%\bookmarksetup{draft=true}


%%%%%%%%%%sq
% SQUEEZE

%% Caption spacing (package caption)
%\captionsetup{aboveskip=0pt}
%\captionsetup[table]{belowskip=0.5ex}

%% Section title spacing (package titlesec)
%\titlespacing*{\section}{0pt}{2ex plus 1.6ex minus 0.6ex}{1ex plus 0.8ex minus 0.2ex}
%\titlespacing*{\subsection}{0pt}{1.6ex plus 1.2ex minus 0.4ex}{1ex plus 0.8ex minus 0.2ex}


%%%%%%%%%%conf
% DOCUMENT CONFIGURATION

%% Page layout (package geometry)
\geometry{margin=1in}

%% Line spacing (package setspace)
%\doublespacing

%% Make bottom of all pages flush (enable package footmisc with option bottom if disabling this, which works better with the default \raggedbottom)
\flushbottom

%% package glossaries-extra
\glsdisablehyper  % Disable hyperlinks for glossary terms in the text
%\setglossarystyle{tree}  % Enable hierarchical glossaries

%% Fix footnotes appearing on the following page (only enable this if the issue is observed)
%\usepackage{etoolbox}
%\makeatletter
%	\patchcmd\end@float{\@cons\@currlist\@currbox}{\@cons\@currlist\@currbox\global\holdinginserts\@ne}{}{}
%	\apptocmd\@specialoutput{\global\holdinginserts\z@}
%\makeatother

%% Keep floats within their sections (package placeins)
%\let\Oldpart\part \renewcommand{\part}{\FloatBarrier\Oldpart}
%\let\Oldchapter\chapter \renewcommand{\chapter}{\FloatBarrier\Oldchapter}
%\let\Oldsection\section \renewcommand{\section}{\FloatBarrier\Oldsection}
%\let\Oldsubsection\subsection \renewcommand{\subsection}{\FloatBarrier\Oldsubsection}
%\let\Oldsubsubsection\subsubsection \renewcommand{\subsubsection}{\FloatBarrier\Oldsubsubsection}

%% Number sections up to a certain depth only (article defaults to 3, report and book defaults to 2)
%\setcounter{secnumdepth}{2}
%% Show sections in the table of contents up to a certain depth only (article defaults to 3, report and book defaults to 2)
%\setcounter{tocdepth}{3}

%% Change depth of appendices in table of contents and PDF bookmarks (requires dissociating PDF bookmark depth above)
%\hypersetup{bookmarksdepth=subsubsection}  % Dissociate PDF bookmark depth from table of contents depth in preamble (required if changing tocdepth mid-document)
%\appto\appendix{
%	\addtocontents{toc}{\protect\setcounter{tocdepth}{1}}  % Change tocdepth in the middle of the table of contents
%	\hypersetup{bookmarksdepth=1}
%}

%% Modify section numbering format
%\renewcommand{\thesection}{\arabic{section}}
%\renewcommand{\thesubsection}{\arabic{section}(\alph{subsection})}
%\renewcommand{\thesubsubsection}{\arabic{section}(\alph{subsection})(\roman{subsubsection})}

%% Modify appendix numbering format (use smallcaps)
\appto\appendix{
	% For document classes with chapters
	%\renewcommand{\thechapter}{\asc{\Alph{chapter}}}
	%\renewcommand{\thesection}{\asc{\Alph{chapter}}.\arabic{section}}
	%\renewcommand{\thesubsection}{\asc{\Alph{chapter}}.\arabic{section}.\arabic{subsection}}

	% For document classes without chapters
	\renewcommand{\thesection}{\asc{\Alph{section}}}
	\renewcommand{\thesubsection}{\asc{\Alph{section}}.\arabic{subsection}}
	\renewcommand{\thesubsubsection}{\asc{\Alph{section}}.\arabic{subsection}.\arabic{subsubsection}}
}

%% Modify equation numbering format
%\renewcommand{\theequation}{\asc{L}\arabic{equation}}

%% List spacing (package enumitem)
\setlist{noitemsep}

%% Reduce captions font size or modify caption margins (package caption)
%\captionsetup{font=small}
%\captionsetup{margin=\parindent}

%% Use uncertainties in parentheses instead of with plus-minus (package siunitx)
%\sisetup{separate-uncertainty=false}

%% Use en dash for unit ranges instead of the word "to" (package siunitx)
%\sisetup{range-phrase=--, range-units=single}

%% Circled text (package tikz)
%\newcommand*\circled[1]{\tikzifexternalizing{}{\tikzexternaldisable}\tikz[baseline=(char.base)]{\node[shape=circle,draw,inner sep=1.2pt] (char) {#1};}\tikzifexternalizing{}{\tikzexternaldisable}}

%% Simpler menu directions (package menukeys)
%\newmenustylesimple*{simplemenus}{\textsf{\CurrentMenuElement}}[ ‣ ]{gray} \renewmenumacro{\menu}[>]{simplemenus}

%% Times New Roman as main font (package fontspec)
%\setmainfont[Kerning=On, Ligatures={Rare}]{Times New Roman}
%\renewfontfamily\scfont[Kerning=On, Ligatures={Rare}, Scale=0.912871]{Times New Roman}  % ( 1.2^(log_1.2(10) - 0.5) ) / 10


%%%%%%%%%%pre
% PREAMBLE (document-specific definitions)

%% Bibliography file (package biblatex)
%\addbibresource{document.bib}

%% Custom fonts (package fontspec)
%\newfontfamily\extrafont[
%	Path=fonts/, Extension=.ttf, UprightFont=*-600, ItalicFont=*-600italic, BoldFont=*-700, BoldItalicFont=*-700italic,
%	Kerning=On, Ligatures={Common}, Diacritics={MarkToBase, MarkToMark}, Scale=MatchLowercase,
%]{source-sans-pro}

%% Colours (package xcolor)
\definecolor{mintedKeyword}{HTML}{0000ff}
\definecolor{mintedType}{HTML}{2b91af}
\definecolor{mintedComment}{HTML}{008000}

%% Custom formatting commands
\newcommand*{\code}[1]{\texttt{#1}}

%% Custom environments
%\newenvironment*{specs}[1]{
%	\vspace{\topsep + \parskip + \partopsep}\begin{tabular}{@{} #1}
%}{
%	\end{tabular}\vspace{\topsep + \parskip + \partopsep}
%}

%% Custom maths constants
\renewcommand*{\d}{\symit{d}}
\newcommand*{\e}{e}
%\renewcommand*{\i}{\symrm{j}}
\newcommand*{\cpi}{\pi}
\newcommand*{\R}{\symbb{R}}

%% Custom maths operators (similar to nabla, sin, etc.)
\DeclareMathOperator{\sign}{sign}
\DeclareMathOperator{\pdf}{\symit{p}}
%% DeclareMathOperator* places subscripts underneath
\DeclareMathOperator*{\argmin}{argmin}
\DeclareMathOperator*{\argmax}{argmax}

%% Maths macros
%\newcommand*{\xv}{\symbf{x}}

%% Text macros; \xspace prevents automatic gobbling of subsequent whitespace after each use of the macro (package xspace)
%\newcommand*{\supercali}{supercalifragilisticexpialidocious\xspace}

%% Build glossaries in external file (package glossaries-extra)
%\makeglossaries

%% Abbreviations will always be rendered as "short" (package glossaries-extra)
%\newabbreviation{usb}{\asc{USB}}{Universal Serial Bus}

%% Acronyms will be rendered as "long (short)" on first use (unless using \glsunset), then "short" subsequently (package glossaries-extra)
%\newacronym{cdf}{\asc{CDF}}{cumulative density function}
%\newacronym{ebnf}{\asc{EBNF}}{extended Backus--Naur form}
%\newacronym{gui}{\asc{GUI}}{graphical user interface} \glsunset{gui}


%%%%%%%%%%doc
\begin{document}
	\title{Title}
	\author{Author\thanks{Author information}}
	\affil{Institution}
	\date{1 January 1970}

	%\setlength{\droptitle}{-1\baselineskip}

	\maketitle

	\section{Introduction}

		Introductory text

	\section{Main section}

		Main text

\end{document}
